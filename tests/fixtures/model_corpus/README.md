# Model parity corpus

This directory intentionally contains no downloaded weights. Add only redistributable,
reviewed fixtures. Real-ESRGAN release evidence must record exact SafeTensors hashes,
the official inference reference, Spandrel version, precision, device, and per-image
pixel-difference evidence. Until that corpus and weight-license review exist, every
workspace registry entry remains disabled.
